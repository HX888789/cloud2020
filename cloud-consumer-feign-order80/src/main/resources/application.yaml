spring:
  application:
    name: cloud-consumer-feign-order80
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:7001/eureka,http://localhost:7002/eureka
server:
  port: 80
logging:
  level:
    com.hx.cloud.service.PaymentFeignService: debug